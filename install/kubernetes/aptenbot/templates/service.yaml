apiVersion: v1
kind: Service
metadata:
  name: {{ include "aptenbot.fullname" . }}
  labels:
    {{- include "aptenbot.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "aptenbot.selectorLabels" . | nindent 4 }}
